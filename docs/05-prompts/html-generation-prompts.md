# HTML Generation Prompts (Legacy Wrapper)

This file is retained for compatibility.

Use `docs/05-prompts/html-summary-generation.md` as the canonical prompt for section HTML summary generation.

Why:
- It contains the mandatory HTML Coverage Check.
- It requires Source Coverage manifest output inside HTML.
- It enforces Product-summary style baseline and defect policy for missing coverage.
