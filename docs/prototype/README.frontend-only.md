# Frontend-Only Track (Legacy)

This track is retained for historical reference.

## Current status
The prototype is no longer frontend-only for runtime flows.
- S02-S09 (except auth) are backend-connected.
- S01 auth remains mocked intentionally.

Use `docs/prototype/README.md` as the active entry point.
